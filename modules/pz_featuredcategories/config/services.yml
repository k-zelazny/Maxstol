services:
  _defaults:
    public: true
    autowire: true
    autoconfigure: true

  PrestaZone\FeaturedCategories\Repository\FeaturedCategoryRepository:
    arguments:
      $connection: '@doctrine.dbal.default_connection'
      $dbPrefix: '%database_prefix%'
