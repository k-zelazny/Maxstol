services:
  _defaults:
    public: true
    autowire: true
    autoconfigure: true

  PrestaZone\ProductConfigurator\Repository\ProductConfiguratorRepository:
    arguments:
      $connection: '@doctrine.dbal.default_connection'
      $dbPrefix: '%database_prefix%'
